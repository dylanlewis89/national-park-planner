<br/>
<%= link_to 'Plan another park', planner_path %>

<h3>Your top parks:</h3>
<%= render partial: 'list_park_scores', object: @sorted_parks %>

<br/>
<h3>Selected region: <%= @input_region.name %></h3>

<%= render partial: 'list_region_states', object: @input_region %>
<br/>
<h3>Selected activities:</h3>
<%= render partial: 'list_activities', object: @input_activities %>


<%# sorted_parks = [] -%>
<%# park_scores.each do |park, info| -%>
	<%# sorted_parks.push([park, info[:score]]) %>
<%#- end -%>
<%#- sorted_parks.sort! { |a,b| a[1] <=> b[1] } -%>