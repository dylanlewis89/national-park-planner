<h1><%= @user.username %></h1>

<br/>
<% if @user == current_user() or current_user().admin? %>
	<%= link_to 'Edit', edit_user_path(@user) %>
<% end %>

<br/><br/>

<%= render partial: 'list_favorite_parks', object: @user %>
<br/>
<%= render partial: 'list_user_trips', object: @user %>
